# 🔧 Обновление SDK - Миграция на официальный Yandex Cloud ML SDK

## 📋 Что изменилось?

Проект обновлён для использования **официального Yandex Cloud ML SDK** вместо прямых HTTP запросов через `httpx`.

### ✅ Преимущества нового подхода

1. **Стабильность API** - официальный SDK поддерживается Yandex
2. **Автоматический retry** - SDK сам обрабатывает повторные попытки при ошибках
3. **Поддержка асинхронных операций** - корректная работа с длительными запросами через `run_deferred()`
4. **Лучшая обработка ошибок** - встроенная обработка rate limits и других ошибок
5. **Актуальность** - автоматические обновления при изменении API

### 🔄 Что было изменено

#### 1. **requirements.txt**
- ✅ Добавлена зависимость: `yandex-cloud-ml-sdk>=0.2.0`
- ✅ Обновлён `python-telegram-bot` до версии `>=21.0` (для совместимости с httpx>=0.27)
- ✅ Обновлён `httpx` до версии `>=0.27.0` (требование SDK)
- ✅ Убраны жесткие версии зависимостей для лучшей совместимости
- ⚠️ **ВАЖНО:** Версии пакетов обновлены для разрешения конфликтов

#### 2. **src/llm/client.py**
- ✅ Заменён `httpx.AsyncClient` на `YCloudML` SDK
- ✅ Используется `model.run_deferred()` для асинхронных запросов
- ✅ Убрана ручная обработка retry (SDK делает это автоматически)
- ✅ Добавлена конвертация результатов SDK в формат, совместимый с `ResponseProcessor`

#### 3. **Совместимость**
- ✅ API клиента остался прежним (`send_prompt()`)
- ✅ `ResponseProcessor` работает без изменений
- ✅ Все режимы диалога работают как прежде
- ⚠️ **ВАЖНО:** Требуется полная переустановка зависимостей

## 🚀 Инструкция по обновлению

### ⚠️ ВАЖНО: Требуется полная переустановка окружения

Из-за конфликта версий `httpx` (SDK требует >=0.27, старый telegram-bot требовал ~=0.25) необходимо:
1. Удалить старое виртуальное окружение
2. Создать новое
3. Установить обновленные зависимости

### Шаг 1: Остановить бота (если запущен)

```bash
# Нажмите Ctrl+C в терминале где запущен бот
```

### Шаг 2: Удалить старое виртуальное окружение

```bash
# Удалить папку venv
rm -rf venv
```

### Шаг 3: Создать новое виртуальное окружение

```bash
# Создать новое venv
python3 -m venv venv

# Активировать (Linux/Mac)
source venv/bin/activate

# Активировать (Windows)
venv\Scripts\activate
```

### Шаг 4: Обновить pip

```bash
pip install --upgrade pip
```

### Шаг 5: Установить зависимости

```bash
pip install -r requirements.txt
```

Вы должны увидеть установку новых пакетов:

```
Collecting python-telegram-bot[job-queue]>=21.0
Collecting httpx>=0.27.0
Collecting yandex-cloud-ml-sdk>=0.2.0
...
Successfully installed httpx-0.27.x python-telegram-bot-21.x yandex-cloud-ml-sdk-0.x.x ...
```

### Шаг 6: Проверить установку

```bash
python -c "from yandex_cloud_ml_sdk import YCloudML; print('✅ SDK установлен успешно!')"
python -c "import telegram; print('✅ Telegram Bot API установлен успешно!')"
```

Если увидели оба `✅` - всё в порядке!

### Шаг 7: Запустить бота

```bash
# Через скрипт (рекомендуется)
./start.sh

# Или напрямую
python run.py
```

### Шаг 8: Проверить работу

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте любое сообщение
4. Проверьте что бот отвечает как обычно

## 🧪 Тестирование после обновления

### Базовый тест

```
/start          - Проверка приветствия
Привет!         - Проверка обычного режима
/science        - Переключение режима
Что такое ДНК?  - Проверка научного режима
```

### Проверка логов

```bash
# Смотрите логи в реальном времени
tail -f logs/rick_bot.log
```

Вы должны увидеть:

```
Initializing YCloudML SDK
Using model: yandexgpt
Sending request to Yandex LLM API: X messages
Received response from Yandex LLM API
```

## ⚠️ Возможные проблемы и решения

### Проблема 1: "ModuleNotFoundError: No module named 'yandex_cloud_ml_sdk'"

**Причина:** SDK не установлен

**Решение:**

```bash
pip install yandex-cloud-ml-sdk --upgrade
```

### Проблема 2: "TypeError: ... object is not callable"

**Причина:** Старая версия SDK или несовместимость

**Решение:**

```bash
pip uninstall yandex-cloud-ml-sdk
pip install yandex-cloud-ml-sdk --upgrade
```

### Проблема 3: Бот не отвечает на сообщения

**Причина:** Возможно проблема с API ключом или Folder ID

**Решение:**

1. Проверьте `.env` файл
2. Убедитесь что `YANDEX_API_KEY` и `YANDEX_FOLDER_ID` правильные
3. Проверьте логи: `cat logs/rick_bot.log | tail -20`

### Проблема 4: "HTTP 401 Unauthorized"

**Причина:** Неверный API ключ

**Решение:**

1. Проверьте API ключ в [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Создайте новый API ключ если нужно
3. Обновите `.env` файл
4. Перезапустите бота

### Проблема 5: SSL Certificate errors

**Причина:** Корпоративный прокси

**Решение:** См. [SSL_FIX.md](SSL_FIX.md)

```bash
# Быстрое решение
echo "SSL_VERIFY=false" >> .env
```

## 📊 Сравнение производительности

### До (httpx)

```
Запрос → HTTP POST → Ждём ответ → Парсим JSON → Готово
⏱️ ~2-5 секунд (в зависимости от нагрузки)
❌ Ручной retry
❌ Ручная обработка ошибок
```

### После (SDK)

```
Запрос → SDK.run_deferred() → Ждём операцию → Готово
⏱️ ~2-5 секунд (аналогично)
✅ Автоматический retry
✅ Встроенная обработка ошибок
✅ Поддержка операций
```

## 🔍 Технические детали

### Как работает новая реализация

1. **Инициализация SDK:**

```python
self.sdk = YCloudML(
    folder_id=folder_id,
    auth=api_key
)
```

2. **Создание модели с настройками:**

```python
model = self.sdk.models.completions("yandexgpt").configure(
    temperature=0.8,
    max_tokens=2000
)
```

3. **Отправка запроса (асинхронно):**

```python
operation = model.run_deferred(messages)
result = await asyncio.get_event_loop().run_in_executor(
    None,
    operation.wait
)
```

4. **Конвертация результата:**

```python
# SDK возвращает GPTModelResult
# Конвертируем в формат для ResponseProcessor
response_dict = self._convert_sdk_result_to_dict(result)
```

### Совместимость с существующим кодом

Метод `send_prompt()` остался прежним:

```python
response = await llm_client.send_prompt(messages)
```

Это означает что **весь остальной код работает без изменений!**

## ✅ Проверочный список

- [ ] Остановлен бот
- [ ] Активировано venv
- [ ] Обновлены зависимости (`pip install -r requirements.txt --upgrade`)
- [ ] SDK установлен успешно
- [ ] Бот запущен
- [ ] Протестированы базовые команды
- [ ] Проверены логи

## 📝 Что дальше?

После успешного обновления:

1. **Мониторьте логи** - первые несколько запросов для проверки
2. **Тестируйте все режимы** - убедитесь что все режимы работают
3. **Проверьте производительность** - должна быть аналогична предыдущей версии

## 🔗 Полезные ссылки

- [Yandex Cloud ML SDK Documentation](https://github.com/yandex-cloud/yandex-cloud-ml-sdk)
- [Yandex Cloud Console](https://console.cloud.yandex.ru)
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем

---

**Вопросы?** Проверьте [TROUBLESHOOTING.md](TROUBLESHOOTING.md) или создайте issue!

*urp* Теперь у нас правильная реализация, Морти! Wubba Lubba Dub Dub! 🧪

