# 🔧 Troubleshooting - Решение проблем

*urp* Если что-то пошло не так, вот как это исправить!

## 🚨 Типичные ошибки и решения

### 1. NetworkError / ConnectError при запуске

**Ошибка:**
```
telegram.error.NetworkError: httpx.ConnectError
```

**Причины:**
- ❌ Нет подключения к интернету
- ❌ Неверный токен Telegram бота
- ❌ Telegram API временно недоступен
- ❌ Проблемы с прокси/файрволом

**Решение:**

#### Шаг 1: Проверьте интернет
```bash
# Проверить подключение к Telegram
ping -c 3 api.telegram.org

# Если не работает, проверьте общее подключение
ping -c 3 google.com
```

#### Шаг 2: Проверьте токен бота
```bash
# Откройте .env
cat .env | grep TELEGRAM_BOT_TOKEN

# Токен должен иметь формат:
# 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Если токен неверный:**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Нажмите "API Token"
5. Скопируйте новый токен в `.env`

#### Шаг 3: Проверьте формат .env
```bash
# .env должен выглядеть так (без пробелов вокруг =)
TELEGRAM_BOT_TOKEN=123456789:ABCdef...
YANDEX_API_KEY=AQVN...
YANDEX_FOLDER_ID=b1...
```

❌ **НЕПРАВИЛЬНО:**
```
TELEGRAM_BOT_TOKEN = 123456789:ABCdef...  # Пробелы!
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here  # Не заменили!
```

✅ **ПРАВИЛЬНО:**
```
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

---

### 2. ValidationError: TELEGRAM_BOT_TOKEN

**Ошибка:**
```
pydantic.ValidationError: TELEGRAM_BOT_TOKEN не указан или содержит значение по умолчанию
```

**Причина:**
Токен не указан или содержит значение-заглушку `your_telegram_bot_token_here`

**Решение:**
```bash
# 1. Откройте .env
nano .env

# 2. Замените заглушку на реальный токен
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# 3. Сохраните файл (Ctrl+O, Enter, Ctrl+X)

# 4. Запустите снова
./start.sh
```

---

### 3. ValidationError: YANDEX_API_KEY

**Ошибка:**
```
pydantic.ValidationError: YANDEX_API_KEY не указан
```

**Причина:**
API ключ Yandex Cloud не указан или неверный

**Решение:**

#### Шаг 1: Получите API ключ

1. Зайдите в [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Выберите или создайте каталог (folder)
3. Перейдите в **"Управление доступом"** → **"Сервисные аккаунты"**
4. Создайте сервисный аккаунт с ролью `ai.languageModels.user`
5. Создайте **API ключ** для этого аккаунта
6. Скопируйте ключ (начинается с `AQVN` или `t1.`)

#### Шаг 2: Укажите в .env
```bash
# Формат API ключа
YANDEX_API_KEY=AQVN...длинная_строка

# Формат Folder ID (из URL консоли или раздела "Обзор")
YANDEX_FOLDER_ID=b1ghsjum2v37ev6vq36q
```

#### Шаг 3: Проверьте права
API ключ должен иметь роль `ai.languageModels.user` для доступа к LLM API.

---

### 4. HTTP 401 Unauthorized (Yandex API)

**Ошибка:**
```
httpx.HTTPStatusError: 401 Unauthorized
```

**Причина:**
Неверный API ключ или Folder ID

**Решение:**

```bash
# 1. Проверьте API ключ
cat .env | grep YANDEX_API_KEY
# Должен начинаться с AQVN или t1.

# 2. Проверьте Folder ID
cat .env | grep YANDEX_FOLDER_ID
# Должен начинаться с b1

# 3. Проверьте что Model URI использует правильный folder_id
cat .env | grep YANDEX_MODEL_URI
# gpt://b1ghsjum2v37ev6vq36q/yandexgpt-lite/latest
#      ^^^^^^^^^^^^^^^^^^ должен совпадать с YANDEX_FOLDER_ID
```

**Пересоздайте API ключ:**
1. Yandex Cloud Console → Сервисные аккаунты
2. Выберите аккаунт
3. Создайте новый API ключ
4. Обновите `.env`

---

### 5. HTTP 429 Too Many Requests

**Ошибка:**
```
httpx.HTTPStatusError: 429 Too Many Requests
```

**Причина:**
Превышен лимит запросов к API

**Решение:**

```bash
# 1. Подождите 1-5 минут
sleep 60

# 2. Проверьте квоты в Yandex Cloud Console
# Консоль → Квоты → Foundation Models

# 3. Увеличьте квоты если нужно
# Консоль → Биллинг → Квоты

# 4. Уменьшите частоту запросов
# В .env добавьте задержку между сообщениями
```

---

### 6. ModuleNotFoundError

**Ошибка:**
```
ModuleNotFoundError: No module named 'telegram'
```

**Причина:**
Зависимости не установлены или venv не активирован

**Решение:**

```bash
# Вариант 1: Используйте start.sh (автоматически установит)
./start.sh

# Вариант 2: Установите вручную
source venv/bin/activate
pip install -r requirements.txt

# Вариант 3: Пересоздайте venv
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

### 7. FileNotFoundError: .env

**Ошибка:**
```
FileNotFoundError: [Errno 2] No such file or directory: '.env'
```

**Причина:**
Файл `.env` не создан

**Решение:**

```bash
# Создайте .env файл
touch .env

# Заполните обязательные поля
cat > .env << 'EOF'
TELEGRAM_BOT_TOKEN=your_token_here
YANDEX_API_KEY=your_api_key_here
YANDEX_FOLDER_ID=your_folder_id_here
EOF

# Отредактируйте и замените заглушки
nano .env
```

---

### 8. Permission denied: ./start.sh

**Ошибка:**
```
bash: ./start.sh: Permission denied
```

**Причина:**
Скрипт не имеет прав на выполнение

**Решение:**

```bash
# Дать права на выполнение
chmod +x start.sh
chmod +x quick-run.sh

# Теперь запустить
./start.sh
```

---

### 9. Python не найден

**Ошибка:**
```
command not found: python3
```

**Причина:**
Python не установлен

**Решение:**

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install python3 python3-venv python3-pip
```

**macOS:**
```bash
# Через Homebrew
brew install python3

# Или скачать с python.org
```

**Проверка:**
```bash
python3 --version
# Должно показать Python 3.10 или выше
```

---

### 10. SSL Certificate Verification Failed

**Ошибка:**
```
httpx.ConnectError: [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: self-signed certificate in certificate chain
```

**Причина:**
Проблема с SSL сертификатом при подключении к Yandex API. Обычно возникает при:
- Корпоративном прокси с самоподписанными сертификатами
- Файрволе с SSL inspection
- Антивирусе с проверкой HTTPS трафика

**Решение 1: Отключить проверку SSL (ТОЛЬКО для разработки)**

```bash
# В .env добавьте
SSL_VERIFY=false
```

⚠️ **ВНИМАНИЕ:** Это небезопасно для продакшена! Используйте только для разработки.

**Решение 2: Установить корпоративные сертификаты (рекомендуется)**

**macOS:**
```bash
# 1. Экспортируйте корпоративный сертификат из Keychain Access
# 2. Установите certifi
pip install --upgrade certifi

# 3. Добавьте сертификат в хранилище certifi
cat /path/to/corporate-cert.pem >> $(python -m certifi)
```

**Ubuntu/Debian:**
```bash
# 1. Скопируйте корпоративный сертификат
sudo cp corporate-cert.crt /usr/local/share/ca-certificates/

# 2. Обновите хранилище сертификатов
sudo update-ca-certificates

# 3. Переустановите requests/httpx в venv
pip install --force-reinstall certifi
```

**Решение 3: Настроить прокси**

```bash
# В .env добавьте (если используете прокси)
HTTP_PROXY=http://proxy.company.com:8080
HTTPS_PROXY=http://proxy.company.com:8080
NO_PROXY=localhost,127.0.0.1
```

**Решение 4: Временно обойти прокси**

```bash
# Запустить без прокси (если возможно)
unset HTTP_PROXY
unset HTTPS_PROXY
./start.sh
```

---

### 11. Не удалось создать venv

**Ошибка:**
```
Error: Command '['/path/to/venv/bin/python3', '-Im', 'ensurepip']' returned non-zero exit status
```

**Причина:**
Модуль venv не установлен (Ubuntu/Debian)

**Решение:**

```bash
# Ubuntu/Debian
sudo apt install python3-venv

# Пересоздать venv
rm -rf venv
python3 -m venv venv
```

---

## 🔍 Отладка

### Включить подробные логи

```bash
# В .env установите
LOG_LEVEL=DEBUG

# Запустите бота
./start.sh

# Логи будут выводиться в консоль и в logs/rick_bot.log
```

### Просмотр логов

```bash
# Последние 50 строк
tail -n 50 logs/rick_bot.log

# Следить за логами в реальном времени
tail -f logs/rick_bot.log

# Поиск ошибок
grep ERROR logs/rick_bot.log
```

### Проверка конфигурации

```bash
# Проверить все переменные окружения
cat .env

# Проверить структуру проекта
tree -L 2 -I 'venv|__pycache__|*.pyc'

# Проверить установленные пакеты
source venv/bin/activate
pip list
```

---

## 📞 Получение помощи

Если проблема не решена:

1. **Проверьте логи:**
   ```bash
   tail -100 logs/rick_bot.log
   ```

2. **Включите DEBUG режим:**
   ```bash
   # .env
   LOG_LEVEL=DEBUG
   ```

3. **Создайте Issue** с описанием:
   - Версия Python (`python3 --version`)
   - Операционная система
   - Полный текст ошибки
   - Логи из `logs/rick_bot.log`

---

## 🎯 Быстрая диагностика

Запустите эту команду для проверки окружения:

```bash
cat > check_env.sh << 'EOF'
#!/bin/bash
echo "=== Проверка окружения ==="
echo ""
echo "Python версия:"
python3 --version
echo ""
echo "Виртуальное окружение:"
if [ -d "venv" ]; then echo "✅ venv существует"; else echo "❌ venv не найден"; fi
echo ""
echo "Файл .env:"
if [ -f ".env" ]; then echo "✅ .env существует"; else echo "❌ .env не найден"; fi
echo ""
if [ -f ".env" ]; then
    echo "Проверка обязательных переменных:"
    if grep -q "TELEGRAM_BOT_TOKEN=" .env && ! grep -q "your_telegram_bot_token_here" .env; then
        echo "✅ TELEGRAM_BOT_TOKEN указан"
    else
        echo "❌ TELEGRAM_BOT_TOKEN не указан или содержит заглушку"
    fi
    if grep -q "YANDEX_API_KEY=" .env && ! grep -q "your_yandex_api_key_here" .env; then
        echo "✅ YANDEX_API_KEY указан"
    else
        echo "❌ YANDEX_API_KEY не указан или содержит заглушку"
    fi
    if grep -q "YANDEX_FOLDER_ID=" .env && ! grep -q "your_yandex_folder_id_here" .env; then
        echo "✅ YANDEX_FOLDER_ID указан"
    else
        echo "❌ YANDEX_FOLDER_ID не указан или содержит заглушку"
    fi
fi
echo ""
echo "Подключение к интернету:"
if ping -c 1 api.telegram.org > /dev/null 2>&1; then
    echo "✅ Доступ к Telegram API"
else
    echo "❌ Нет доступа к Telegram API"
fi
EOF

chmod +x check_env.sh
./check_env.sh
```

---

*urp* Если ничего не помогло - перечитай документацию заново, Морти! Wubba Lubba Dub Dub! 🧪

