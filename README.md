
# 🤖 День 7: Сравнение разных моделей LLM

**Ветка:** `day_7_different_models`

**Описание:** Сравнительное исследование различных LLM моделей на одинаковых запросах с замером производительности и оценкой качества ответов.

## 🔥 День 8: Работа с токенами

**Цель:** показать, как меняется поведение модели при разных объёмах запроса и при подсчёте токенов.

Что сделать:
- Добавить подсчёт токенов для каждого запроса и ответа: вход (input), выход (output), итог (total).
- Логировать вместе: токены, latency, стоимость (если доступна), модель, сценарий, результат (ok / усечение / ошибка).
- Реализовать явную реакцию на превышение лимита: отображать лимит, что произошло (ошибка/усечение/ретрай).
- Подготовить и прогнать три сценария:
  - **Короткий запрос** — базовый.
  - **Длинный запрос** — рядом с лимитом модели.
  - **Запрос выше лимита** — ожидаем ошибка/усечение/ретрай с явным указанием лимита.
- Зафиксировать результаты в метриках/отчёте: latency, input/output/total tokens, cost (если есть), заметки о качестве/поведении.

Как запускать и менять тексты:
1. Указать/обновить тексты трёх промптов в коде или отдельном конфиге (короткий, длинный, превышающий лимит).
2. Запустить приложение (пример: `python run.py` или через Telegram-бота, если используется).
3. В логах/отчёте должны быть видны: модель, сценарий, input/output/total tokens, latency, cost (если есть), статус обработки, явное сообщение при превышении лимита.
4. По итогам сохранить результаты в `metrics` (или отдельном отчёте) с краткими заметками о качестве ответа и поведении при лимите.

## 📖 Описание задания

Вызовите один и тот же запрос на разных моделях (из начала, середины и конца списка HuggingFace) для сравнения:

- **Время ответа** - latency от отправки запроса до получения ответа
- **Количество токенов** - input/output токены
- **Стоимость** - итоговая стоимость запроса (если модель платная)
- **Качество** - субъективная оценка качества ответа

## 🎯 Цели эксперимента

1. **Сравнить производительность** разных моделей на одинаковых задачах
2. **Оценить соотношение цена/качество** для разных моделей
3. **Измерить latency** и понять trade-offs между скоростью и качеством
4. **Сформулировать рекомендации** по выбору модели для разных задач

## 🔬 Технологии

- **Python 3.10+**
- **HTTP API** - для работы со всеми моделями через REST API
- **httpx** - async HTTP клиент для замеров времени
- **python-telegram-bot** - для тестирования через Telegram бота

## 🧪 Эксперименты

### Выбор моделей

Для сравнения выбраны модели из разных провайдеров:

| Модель | Провайдер | Размер | Тип |
|--------|-----------|--------|-----|
| **Claude 4.5** | Anthropic | Large | Коммерческая |
| **Gpt 4o mini** | OpenAI | Small | Коммерческая |
| **Grok 2** | xAI | Large | Коммерческая |

### Тестовые запросы

Для сравнения используются одинаковые запросы разных типов:

1. **Фактологический вопрос** - проверка точности знаний
2. **Творческая задача** - генерация текста/идей
3. **Логическая задача** - рассуждение и анализ
4. **Код** - генерация/объяснение кода

### Метрики для измерения

| Метрика | Описание | Единица измерения |
|---------|----------|-------------------|
| **Latency** | Время от запроса до ответа | ms |
| **Input Tokens** | Токены во входном запросе | tokens |
| **Output Tokens** | Токены в ответе | tokens |
| **Total Tokens** | Общее количество токенов | tokens |
| **Cost** | Стоимость запроса | $ |
| **Quality Score** | Субъективная оценка качества (0-10) | баллы |
| **Structure** | Структурированность ответа (0-10) | баллы |
| **Creativity** | Креативность/оригинальность (0-10) | баллы |

## 📊 Методология сравнения

### Критерии оценки качества

1. **Точность (Accuracy)** - корректность фактов и логики
2. **Полнота (Completeness)** - насколько полно раскрыт вопрос
3. **Релевантность (Relevance)** - соответствие запросу
4. **Читаемость (Readability)** - качество форматирования и стиля
5. **Скорость (Speed)** - время генерации ответа

### Формула оценки

```
Overall Score = (Accuracy × 0.3) + (Completeness × 0.25) + (Relevance × 0.25) + (Readability × 0.1) + (Speed × 0.1)

```

## 🚀 Быстрый старт

### Требования

- **Python 3.10+**
- **ELIZA_TOKEN** - токен для доступа к API моделей
- **Telegram Bot Token** (от @BotFather) - опционально

### Установка

1. **Клонируйте репозиторий:**

```bash
git clone <repository-url>
cd ai-challenge
git checkout day_7_diffenent_models

```

2. **Создайте виртуальное окружение:**

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac

# или
venv\Scripts\activate  # Windows

```

3. **Установите зависимости:**

```bash
pip install -r requirements.txt

```

4. **Создайте файл `.env`:**

```bash
cp .env.example .env

```

5. **Заполните `.env` файл:**

```env

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API Configuration
ELIZA_TOKEN=your_eliza_token_here

# Models to compare
MODEL_CLAUDE=claude-4-5
MODEL_DEEPSEEK=deepseek-chat
MODEL_GPT=gpt-5.1
MODEL_YANDEXGPT=yandexgpt

# Application Settings
LOG_LEVEL=INFO
MAX_MESSAGE_LENGTH=4000

```

### Запуск экспериментов

#### Ручной запуск

```bash
source venv/bin/activate
python run.py

```

## 📈 Результаты и выводы

### prompt_1 (короткий запрос)

| Модель | Latency (ms) | Input Tokens | Output Tokens | Total Tokens | Cost ($) | Quality (0-10) | Structure (0-10) | Creativity (0-10) |
|--------|--------------|--------------|---------------|--------------|----------|----------------|------------------|-------------------|
| gpt-4o-mini | 1848 | 41 | 71 | 112 | 0.00004875 | N/A | N/A | N/A |
| claude-opus-4-5 | 4614 | 62 | 139 | 201 | 0.003785 | N/A | N/A | N/A |
| grok-2-vision | 2222 | 47 | 48 | 95 | 0.0005740000000000001 | N/A | N/A | N/A |

### prompt_2 (длинный запрос)

| Модель | Latency (ms) | Input Tokens | Output Tokens | Total Tokens | Cost ($) | Quality (0-10) | Structure (0-10) | Creativity (0-10) |
|--------|--------------|--------------|---------------|--------------|----------|----------------|------------------|-------------------|
| gpt-4o-mini | 3809 | 250 | 182 | 432 | 0.0001467 | N/A | N/A | N/A |
| claude-opus-4-5 | 7147 | 344 | 269 | 613 | 0.008445000000000001 | N/A | N/A | N/A |
| grok-2-vision | 11273 | 297 | 220 | 517 | 0.002794 | N/A | N/A | N/A |

### prompt_3 (выше лимита)

| Модель | Latency (ms) | Input Tokens | Output Tokens | Total Tokens | Cost ($) | Quality (0-10) | Structure (0-10) | Creativity (0-10) |
|--------|--------------|--------------|---------------|--------------|----------|----------------|------------------|-------------------|
| gpt-4o-mini | 8429 | 4418 | 500 | 8929 | 0.0009626999999999999 | N/A | N/A | N/A |
| claude-opus-4-5 | 10640 | 6357 | 500 | 6857 | 0.044285000000000005 | N/A | N/A | N/A |
| grok-2-vision | 17689 | 5434 | 500 | 5934 | 0.015868 | N/A | N/A | N/A |

### Вывод

`gpt-4o-mini` показал хорошо себя во всех 3 промтах. Он экономных на входных и выходных токенах. Для 3 промта он почти закончил вывод, пока `grok-2` и `claude-opus-4-5` только были на втором пункте. При этом он на несколько порядков дешевле.

`grok-2` среднячок. Отвечает примерно как `claude-opus-4-5`, но в 3-4 раза дешевле. 

`claude-opus-4-5` - дорого и круто. 

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Обязательна | Описание | По умолчанию |
|------------|-------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | ✅ | Токен Telegram бота | - |
| `ELIZA_TOKEN` | ✅ | Токен для доступа к API моделей | - |

### Просмотр логов

```bash

# Последние логи
tail -50 logs/rick_bot.log

# Следить за логами в реальном времени
tail -f logs/rick_bot.log

# Поиск ошибок
grep ERROR logs/rick_bot.log

```

