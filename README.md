# 🚀 День 29: Локальный аналитик

**Описание:** на этом этапе мы делаем “локального аналитика” — систему, которая подключается к данным проекта (CSV / JSON / логи / SQLite) и отвечает на аналитические вопросы, **не выходя в облако**.

🎯 **Ключевая фича:** модель отвечает на вопросы вида “что чаще всего происходит?” и “где пользователи теряются?” **на основании цифр** (агрегации/топы/конверсии) и умеет объяснить вывод.

## 📌 Что нужно сделать

- **Подключить данные локально** (любой 1+ источник):
  - **JSON**: можно начать с примера `resources/analytics-digest-1777822926768606593.json` (события/метрики).
  - **SQLite**: можно использовать `rick_bot.db` (история/состояния/метаданные — зависит от схемы).
  - **Логи**: если приложение пишет в `logs/`, поддержать парсинг строк (уровень/время/сообщение/ошибка).
  - **CSV**: поддержать базовую загрузку через стандартный модуль `csv`.
- **Нормализовать события в одну “таблицу”** (на уровне кода — список записей/словарей):
  - минимум поля: `timestamp`, `source`, `event_name`/`error_type`, `user_id` (если есть), `payload` (как JSON/строка).
  - предусмотреть “грязные данные”: пропуски, разные форматы времени, неожиданные поля.
- **Сделать слой метрик (без LLM)** — функции, которые считают числа:
  - **топ ошибок**: группировка по `error_type`/`message` + топ‑N + доля/динамика по времени (если возможно).
  - **воронка / drop-off**: выстроить шаги по `event_name` и посчитать конверсию между шагами.
  - опционально: **latency**/длительности (`duration`) — распределение и выбросы.
- **Поверх метрик сделать “аналитический чат”**:
  - вход: вопрос на естественном языке;
  - шаг 1: извлечь намерение (top errors / funnel / latency / фильтры по периоду/событию);
  - шаг 2: посчитать метрики локально;
  - шаг 3: сформировать ответ модели **только по вычисленным агрегатам**, без выдумок.
- **Гарантировать полностью локальный режим**:
  - LLM должна работать через локальный endpoint (например, Ollama по умолчанию `http://localhost:11434/v1` в настройках);
  - запретить любые внешние API для аналитики (никакого облака).

## 🧪 Как проверить

1. Возьмите один источник данных (например, `resources/analytics-digest-1777822926768606593.json`) и прогоните загрузку/нормализацию — убедитесь, что получается список событий и нет падений.
2. Проверьте 3–5 вопросов и сравните ответ с цифрами, которые реально посчитаны:
  - “Какая ошибка встречается чаще всего? Покажи топ‑5.”
    - “Какие события самые частые за последний период?”
    - “Где больше всего пользователей теряется? Считай воронку по шагам X → Y → Z.”
    - “Сколько было событий типа X по дням?”
3. Убедитесь, что ответ содержит:
  - конкретные значения (счётчики/проценты),
    - понятный вывод (почему так),
    - и при необходимости “не хватает данных” вместо фантазий.

## 🧾 Критерии готовности (чеклист)

- **Данные подключены локально**: минимум один источник (CSV/JSON/логи/SQLite) читается и нормализуется ✅
- **Метрики считаются без LLM**: есть минимум 2 вида аналитики (например, топ ошибок + воронка) ✅
- **Модель отвечает по фактам**: в ответах есть цифры и нет выдуманных данных ✅
- **Всё локально**: аналитика не требует внешних облачных сервисов ✅

