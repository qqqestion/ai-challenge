
# 🚀 День 21: Автоматизация ревью кода

**Описание:** Настраиваем CI-пайплайн, где ассистент автоматически ревьюит новый PR: берёт контекст из RAG (документация + код) и через MCP получает diff и файлы PR, формирует рекомендации с цитированием.

✨ **Что нового:**
- 🆕 CI-стадия: ассистент запускается на каждый PR.
- 🆕 RAG по докам и коду для контекстных подсказок.
- 🆕 MCP-доступ к diff и списку файлов PR.
- 🧪 Метрики по качеству подсказок, полноте контекста и времени отклика.

🎯 **Ключевая фича:** Автоматический code review бота в CI, комбинирующий RAG и MCP для точных ссылок на изменения и правила.

📚 **Полезно почитать:**
- Практики RAG (индексация, обновление векторов, сужение контекста)
- MCP-адаптеры для git/PR (diff, список файлов)
- Гайды по проектным соглашениям и стилю кода

## 📖 Описание задания

Задача: «День 21. Автоматизация ревью кода» — подключить ассистента в CI, чтобы он анализировал новые PR, объединяя контекст RAG (документация и код) и MCP (diff, файлы PR) и выдавал рекомендации по изменениям с цитатами.

## 🎯 Цели

1. Подготовить RAG-доступ к докам и коду для ревью.
2. Подключить MCP-сервер, отдающий diff и список файлов PR.
3. Встроить ассистента в CI-джобу, запускаемую на каждый PR.
4. Выдавать рекомендации с цитированием строк/файлов и ссылками на правила стиля.
5. Логировать источники контекста (RAG/MCP), объём и время ответа.
6. Обрабатывать кейсы без релевантного контекста (fallback/предупреждение).

## 🔧 План работ (ожидаемое)

- Обновить/пересобрать векторное хранилище по докам и коду.
- Настроить RAG-пайплайн: поиск, top-k, формат ссылок на файлы/строки.
- Настроить MCP для PR: diff, список изменённых файлов, возможно метаданные (автор, теги).
- Собрать CI-джобу (например, GitHub Actions/runner): шаги установки, запуск ассистента, сохранение отчёта.
- Форматировать вывод: список находок, цитаты, ссылки на правила/код.
- Добавить метрики: количество документов, источники (RAG/MCP), длина ответа, время выполнения, флаг пустого/неполного контекста.

## 🆚 Проверка ассистента

| Критерий | Без бота | С ботом (RAG+MCP) | Комментарий |
| --- | --- | --- | --- |
| Нахождение дефектов |  |  |  |
| Наличие ссылок/цитат |  |  |  |
| Покрытие diff (все файлы) |  |  |  |
| Полезность для ревью |  |  |  |
| Время ответа |  |  |  |
| Итоговый вердикт |  |  |  |

| Сценарий | Ожидаемый ответ бота | Источник контекста | Проверка |
| --- | --- | --- | --- |
| Есть явная ошибка в diff | Описание проблемы + ссылка на строку | MCP (diff) + RAG (правила/код) | Найдено и процитировано |
| Вопрос по стилю/гайду | Ссылка на правило + пример | RAG (доки/стайлгайд) | Правило верное |
| Большой PR | Краткий обзор + список файлов с рисками | MCP (список файлов) | Все файлы учтены |
| Нет контекста в доках | Fallback/предупреждение | RAG | Корректный отказ |

## 🧪 Как проверить

1. Сгенерировать/обновить векторную базу по актуальным докам и коду.
2. Настроить MCP-провайдер для получения diff и списка файлов PR.
3. Запустить CI-джобу на тестовом PR с известной ошибкой — бот должен сослаться на строку diff и правило.
4. Проверить лог: источники (RAG/MCP), число документов, длина ответа, время выполнения.
5. Проверить fallback на вопрос без контекста.
6. Убедиться, что отчёт CI прикреплён к PR (коммент или артефакт).
