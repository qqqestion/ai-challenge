
# 🚀 День 15: Environment (MCP + Android эмулятор)

**Описание:** Соединяем агента с реальным окружением: поднимаем докер/эмулятор и запускаем в нём команды через MCP. План — сделать MCP для Android эмулятора.

✨ **Что нового:**
- ✅ MCP-инструменты управляют окружением (старт/стоп, проверка готовности)
- ✅ Поддержка запуска внутри контейнера или эмулятора
- ✅ Передача вывода/логов обратно в агента
- ✅ Базовые проверки здоровья окружения перед задачами

🎯 **Ключевая фича:** Агент сам поднимает Android эмулятор (или докер с эмулятором) и выполняет нужные действия через MCP.

📚 **Полезно почитать:**
- Документация по базовым инструментам в `github_mcp/README.md`

## 📖 Описание задания

Задача: «День 15. Environment» — связать агента с реальным окружением (докер/эмулятор) и научить его запускать в нём операции через MCP. Фокус — Android эмулятор.

## 🎯 Цели

1. Поднять окружение (докер или нативный Android эмулятор) по запросу агента.  
2. Дать MCP-инструменты для управления жизненным циклом (start/stop/status).  
3. Передавать логи/вывод обратно в агента.  
4. Минимальная проверка готовности (здоровье, порты, сеть).

## 🔧 План работ (ожидаемое)

- Добавить MCP-инструмент(ы) для управления эмулятором (старт/стоп/healthcheck).  
- Подготовить контейнер/скрипт, который разворачивает эмулятор и отдаёт управление через stdio.  
- Вернуть в ответах MCP лог запуска и статус готовности, чтобы агент мог решать, что делать дальше.  
- Опционально: инструмент для выполнения команды/теста внутри эмулятора.

## 🧪 Как проверить (черновик)

```bash
# 1) Поднять окружение (пример, будет уточнён после реализации)
mcp-tool start_emulator --image android-emulator:latest --avd Pixel_6

# 2) Проверить статус
mcp-tool emulator_status

# 3) Выполнить тестовую команду внутри (пример)
mcp-tool exec_in_emulator --cmd "adb devices"
```

Ожидаемый результат: MCP возвращает логи старта, видит эмулятор в списке, команды внутри выполняются, агент получает вывод.

