# 🤖 День 7: Сравнение разных моделей LLM

**Ветка:** `day_7_diffenent_models`

**Описание:** Сравнительное исследование различных LLM моделей на одинаковых запросах с замером производительности и оценкой качества ответов.

## 📖 Описание задания

Вызовите один и тот же запрос на разных моделях (из начала, середины и конца списка HuggingFace) для сравнения:

- **Время ответа** - latency от отправки запроса до получения ответа
- **Количество токенов** - input/output токены
- **Стоимость** - итоговая стоимость запроса (если модель платная)
- **Качество** - субъективная оценка качества ответа

## 🎯 Цели эксперимента

1. **Сравнить производительность** разных моделей на одинаковых задачах
2. **Оценить соотношение цена/качество** для разных моделей
3. **Измерить latency** и понять trade-offs между скоростью и качеством
4. **Сформулировать рекомендации** по выбору модели для разных задач

## 🔬 Технологии

- **Python 3.10+**
- **HTTP API** - для работы со всеми моделями через REST API
- **httpx** - async HTTP клиент для замеров времени
- **python-telegram-bot** - для тестирования через Telegram бота

## 🧪 Эксперименты

### Выбор моделей

Для сравнения выбраны модели из разных провайдеров:

| Модель | Провайдер | Размер | Тип |
|--------|-----------|--------|-----|
| **Claude 4.5** | Anthropic | Large | Коммерческая |
| **DeepSeek** | DeepSeek | Large | Коммерческая/Open |
| **GPT 5.1** | OpenAI | Large | Коммерческая |
| **YandexGPT** | Yandex Cloud | Large | Коммерческая |

### Тестовые запросы

Для сравнения используются одинаковые запросы разных типов:

1. **Фактологический вопрос** - проверка точности знаний
2. **Творческая задача** - генерация текста/идей
3. **Логическая задача** - рассуждение и анализ
4. **Код** - генерация/объяснение кода

### Метрики для измерения

| Метрика | Описание | Единица измерения |
|---------|----------|-------------------|
| **Latency** | Время от запроса до ответа | ms |
| **Input Tokens** | Токены во входном запросе | tokens |
| **Output Tokens** | Токены в ответе | tokens |
| **Total Tokens** | Общее количество токенов | tokens |
| **Cost** | Стоимость запроса | $ |
| **Quality Score** | Субъективная оценка качества | 1-5 |

## 📊 Методология сравнения

### Критерии оценки качества

1. **Точность (Accuracy)** - корректность фактов и логики
2. **Полнота (Completeness)** - насколько полно раскрыт вопрос
3. **Релевантность (Relevance)** - соответствие запросу
4. **Читаемость (Readability)** - качество форматирования и стиля
5. **Скорость (Speed)** - время генерации ответа

### Формула оценки

```
Overall Score = (Accuracy × 0.3) + (Completeness × 0.25) + (Relevance × 0.25) + (Readability × 0.1) + (Speed × 0.1)
```

## 🚀 Быстрый старт

### Требования

- **Python 3.10+**
- **ELIZA_TOKEN** - токен для доступа к API моделей
- **Telegram Bot Token** (от @BotFather) - опционально

### Установка

1. **Клонируйте репозиторий:**

```bash
git clone <repository-url>
cd ai-challenge
git checkout day_7_diffenent_models
```

2. **Создайте виртуальное окружение:**

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`:**

```bash
cp .env.example .env
```

5. **Заполните `.env` файл:**

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API Configuration
ELIZA_TOKEN=your_eliza_token_here

# Models to compare
MODEL_CLAUDE=claude-4-5
MODEL_DEEPSEEK=deepseek-chat
MODEL_GPT=gpt-5.1
MODEL_YANDEXGPT=yandexgpt

# Application Settings
LOG_LEVEL=INFO
MAX_MESSAGE_LENGTH=4000
```

### Запуск экспериментов

#### Автоматический запуск

```bash
chmod +x start.sh
./start.sh
```

#### Ручной запуск

```bash
source venv/bin/activate
python run.py
```

## 📈 Результаты и выводы

### Сравнительная таблица производительности

| Модель | Latency (ms) | Input Tokens | Output Tokens | Total Tokens | Cost ($) | Quality Score |
|--------|--------------|--------------|---------------|--------------|----------|---------------|
| **Claude 4.5** | TBD | TBD | TBD | TBD | TBD | TBD |
| **DeepSeek** | TBD | TBD | TBD | TBD | TBD | TBD |
| **GPT 5.1** | TBD | TBD | TBD | TBD | TBD | TBD |
| **YandexGPT** | TBD | TBD | TBD | TBD | TBD | TBD |

### Детальное сравнение по критериям

| Модель | Точность | Полнота | Релевантность | Читаемость | Скорость | Overall Score |
|--------|----------|---------|--------------|------------|----------|---------------|
| **Claude 4.5** | TBD | TBD | TBD | TBD | TBD | TBD |
| **DeepSeek** | TBD | TBD | TBD | TBD | TBD | TBD |
| **GPT 5.1** | TBD | TBD | TBD | TBD | TBD | TBD |
| **YandexGPT** | TBD | TBD | TBD | TBD | TBD | TBD |

## 🏗️ Архитектура проекта

```
ai-challenge/
├── src/
│   ├── bot/                    # Telegram bot модули
│   │   ├── bot.py              # Основной класс бота
│   │   ├── handlers.py         # Обработчики команд
│   │   └── message_processor.py # Обработка сообщений
│   ├── llm/                    # LLM API интеграция
│   │   ├── client.py           # Multi-model HTTP клиент
│   │   ├── metrics.py          # Сбор метрик
│   │   └── prompts.py          # Промпты для тестов
│   ├── config/                 # Конфигурация
│   │   ├── settings.py         # Настройки приложения
│   │   └── logger.py           # Логирование
│   └── main.py                 # Точка входа
├── experiments/                # Результаты экспериментов
│   └── results.json            # JSON с метриками
├── logs/                       # Логи
├── requirements.txt            # Python зависимости
├── .env                        # Переменные окружения
└── README.md                   # Этот файл
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Обязательна | Описание | По умолчанию |
|------------|-------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | ❌ | Токен Telegram бота | - |
| `ELIZA_TOKEN` | ✅ | Токен для доступа к API моделей | - |
| `LOG_LEVEL` | ❌ | Уровень логирования | `INFO` |

## 📚 Документация

- **[PLAN.md](PLAN.md)** - Детальный план разработки
- **[PROJECT_STATUS.md](PROJECT_STATUS.md)** - Текущий статус проекта
- **[EXPERIMENTS.md](EXPERIMENTS.md)** - Детальные результаты экспериментов

## 🐛 Troubleshooting

### Типичные проблемы

| Проблема | Быстрое решение |
|----------|-----------------|
| **HTTP 401 Unauthorized** | Проверьте ELIZA_TOKEN в .env файле |
| **Model not found** | Проверьте правильность названий моделей |
| **Rate limit exceeded** | Подождите или проверьте лимиты API |
| **Timeout** | Увеличьте timeout или проверьте сеть |
| **Invalid token** | Проверьте корректность ELIZA_TOKEN в .env файле |

### Просмотр логов

```bash
# Последние логи
tail -50 logs/rick_bot.log

# Следить за логами в реальном времени
tail -f logs/rick_bot.log

# Поиск ошибок
grep ERROR logs/rick_bot.log
```

## 📄 Лицензия

Этот проект создан в рамках AI Challenge.

---

**Результат:** ⏳ В процессе - сравнение моделей, сбор метрик, анализ результатов

*Последнее обновление: день 7*

## ℹ️ Обновление клиента

- LLM клиент переведён на REST API `https://api.eliza.yandex.net/openai/v1/chat/completions`
- Используется модель `gpt-4.1-nano` по умолчанию, авторизация через `ELIZA_TOKEN` (OAuth)
