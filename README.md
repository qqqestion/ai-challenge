
# 🚀 День 28: Оптимизация и адаптация локальной LLM

**Описание:** на этом этапе мы улучшаем качество и управляемость ответов локальной LLM под конкретную задачу за счёт (1) настройки параметров инференса и (2) более точного prompt-шаблона.

🎯 **Ключевая фича:** “тюнинг” поведения модели без смены архитектуры — через параметры и промпт, с проверкой на одинаковом наборе запросов “до/после”.

## 📌 Что нужно сделать

- **Зафиксировать baseline**:
  - выбрать 5–10 типовых запросов для вашей задачи (одинаковые для всех прогонов);
  - зафиксировать текущие параметры: квантование (если есть), контекстное окно, `temperature`, `max_tokens`.
- **Потрогать параметры модели** (менять по одному параметру за раз):
  - **квантование**: сравнить 2 режима (например, более “лёгкое” и более “точное”);
  - **контекстное окно**: проверить, как изменение окна влияет на качество и скорость;
  - **temperature**: проверить минимум 2–3 значения (например, 0.0 / 0.3 / 0.7);
  - **max tokens**: подобрать значение, чтобы ответы были достаточно полными, но без “воды”.
- **Настроить prompt-шаблон под задачу**:
  - явно задать роль, ограничения и ожидаемый формат ответа;
  - добавить “правила качества” (например: не выдумывать факты, задавать уточняющий вопрос при нехватке данных);
  - при необходимости добавить примеры вход/выход (few-shot) и сравнить “до/после”.
- **Сравнить результаты**:
  - прогнать один и тот же набор запросов на baseline и на лучших настройках;
  - записать вывод: какие настройки дали лучший результат и почему.

## 🧪 Как проверить

1. Запустите одинаковый набор тестовых запросов на baseline-настройках и сохраните ответы.
2. По очереди меняйте один параметр (квантование / контекст / temperature / max tokens) и повторяйте прогон.
3. Обновите prompt-шаблон, повторите прогон и сравните качество ответов с baseline.

## 🧾 Критерии готовности (чеклист)

- **Baseline зафиксирован**: есть набор запросов и исходные параметры ✅
- **Параметры протестированы**: есть минимум 2 прогона на разные значения (и понятно, что изменялось) ✅
- **Prompt-шаблон улучшен**: есть более точный шаблон под задачу (и он реально влияет на ответы) ✅
- **Есть выводы “до/после”**: зафиксированы лучшие настройки и их эффект ✅

