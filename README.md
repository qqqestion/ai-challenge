# Задание 5: Изменение systemPrompt в процессе диалога

## 📖 Описание задания

**Цель:** Исследовать влияние системного промпта (systemPrompt) на поведение LLM-агента в процессе диалога.

**Задачи:**
1. Задать агенту systemPrompt и сделать несколько шагов в диалоге с агентом
2. В ходе работы поменять systemPrompt и продолжить диалог
3. Сравнить как меняется реакция агента с изменением systemPrompt

## 🎯 Что такое systemPrompt?

**SystemPrompt (системный промпт)** — это инструкция для LLM, которая определяет:
- **Личность агента** — как он себя ведёт и общается
- **Стиль ответов** — формальный/неформальный, саркастичный/дружелюбный
- **Формат ответов** — структура и правила генерации
- **Контекст и ограничения** — что агент должен и не должен делать

Изменение systemPrompt в процессе диалога позволяет:
- Наблюдать, как агент адаптируется к новой роли
- Сравнивать реакции на одни и те же вопросы с разными промптами
- Исследовать влияние промпта на контекст диалога

## 🏗️ Как это реализовано в проекте

### Структура проекта

```
src/
├── llm/
│   ├── modes.py              # ⭐ Системные промпты здесь
│   ├── client.py             # Клиент Yandex LLM API
│   └── response_processor.py # Обработка ответов
├── bot/
│   ├── llm_integration.py    # Интеграция бота с LLM
│   └── handlers.py          # Обработчики команд
└── main.py                   # Точка входа
```

### Где находится systemPrompt

Системные промпты определены в `src/llm/modes.py`:

```python
class ModePromptBuilder:
    _MODE_SYSTEM_PROMPTS: Dict[RickMode, str] = {
        RickMode.NORMAL: """
        <prompt>
            <persona>
                <name>Рик Санчез (C-137)</name>
                <description>Ты — гениальный, циничный ученый...</description>
            </persona>
            ...
        </prompt>
        """
    }
```

### Как работает изменение промпта

1. **При обработке сообщения** (`src/bot/llm_integration.py`):
   - Получается текущий режим (mode)
   - Через `build_mode_prompt()` извлекается systemPrompt для режима
   - SystemPrompt передаётся в LLM вместе с сообщением пользователя

2. **Для изменения промпта** можно:
   - Редактировать `_MODE_SYSTEM_PROMPTS` в `modes.py`
   - Добавить новый режим в `RickMode` enum
   - Изменить промпт существующего режима

## 🧪 Как экспериментировать

### Способ 1: Изменение промпта в коде

1. **Откройте файл** `src/llm/modes.py`

2. **Найдите словарь** `_MODE_SYSTEM_PROMPTS`:

```python
_MODE_SYSTEM_PROMPTS: Dict[RickMode, str] = {
    RickMode.NORMAL: """
    <prompt>
        <!-- Ваш промпт здесь -->
    </prompt>
    """
}
```

3. **Измените промпт** на нужный вам:

```python
_MODE_SYSTEM_PROMPTS: Dict[RickMode, str] = {
    RickMode.NORMAL: """
    <prompt>
        <persona>
            <name>Дружелюбный помощник</name>
            <description>Ты — вежливый и отзывчивый помощник...</description>
        </persona>
    </prompt>
    """
}
```

4. **Перезапустите бота** и продолжите диалог

### Способ 2: Добавление нового режима

1. **Добавьте новый режим** в enum `RickMode`:

```python
class RickMode(str, Enum):
    NORMAL = "normal"
    FRIENDLY = "friendly"  # Новый режим
```

2. **Добавьте промпт** для нового режима:

```python
_MODE_SYSTEM_PROMPTS: Dict[RickMode, str] = {
    RickMode.NORMAL: "...",
    RickMode.FRIENDLY: """
    <prompt>
        <persona>
            <name>Дружелюбный помощник</name>
            ...
        </persona>
    </prompt>
    """
}
```

3. **Измените режим** в `llm_integration.py`:

```python
# Было:
current_mode = RickMode.NORMAL

# Стало:
current_mode = RickMode.FRIENDLY
```

## 📝 Примеры экспериментов

### Эксперимент 1: Смена личности

**Шаг 1:** Начните диалог с промптом "Рик Санчез"
- Задайте вопрос: "Расскажи о квантовой физике"
- Зафиксируйте ответ

**Шаг 2:** Измените промпт на "Дружелюбный учитель"
- Задайте тот же вопрос: "Расскажи о квантовой физике"
- Сравните ответы

**Ожидаемый результат:** Ответы должны отличаться по тону, стилю и глубине объяснения.

### Эксперимент 2: Изменение формата ответов

**Шаг 1:** Промпт с JSON-форматом ответов
```xml
<rules>
    <rule>Твой ответ должен быть в формате JSON: {"answer": "...", "emotion": "..."}</rule>
</rules>
```

**Шаг 2:** Промпт с обычным текстом
```xml
<rules>
    <rule>Отвечай обычным текстом, без структурирования</rule>
</rules>
```

**Ожидаемый результат:** Первый промпт генерирует структурированные ответы, второй — свободный текст.

### Эксперимент 3: Влияние на контекст диалога

**Шаг 1:** Начните диалог с промптом "Научный эксперт"
- Задайте несколько вопросов
- Постройте контекст разговора

**Шаг 2:** Смените промпт на "Художник-творец"
- Продолжите диалог с теми же темами
- Наблюдайте, как агент интерпретирует предыдущий контекст

**Ожидаемый результат:** Агент может по-разному интерпретировать предыдущие сообщения в зависимости от новой роли.

## 🚀 Быстрый старт

### Требования

- **Python 3.10+**
- **Telegram Bot Token** (от @BotFather)
- **Yandex Cloud API Key** (от Yandex Cloud AI Studio)

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd ai-challenge
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env`:**
```bash
cp .env.example .env
```

5. **Заполните `.env` файл:**
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
YANDEX_API_KEY=your_yandex_api_key_here
YANDEX_FOLDER_ID=your_yandex_folder_id_here
YANDEX_MODEL_URI=gpt://your_folder_id/yandexgpt-lite/latest
```

### Запуск

```bash
# Автоматическая настройка и запуск
./start.sh

# Или быстрый запуск (если venv уже настроен)
./quick-run.sh

# Или вручную
python run.py
```

## 🔑 Получение API ключей

### Telegram Bot Token

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### Yandex Cloud API Key

1. Зарегистрируйтесь в [Yandex Cloud](https://cloud.yandex.ru)
2. Создайте каталог (folder)
3. Перейдите в [AI Studio](https://console.cloud.yandex.ru/folders/<folder_id>/foundation-models)
4. Получите API ключ в разделе "API ключи"
5. Скопируйте `folder_id` и `api_key` в `.env`

## 📊 Сравнение реакций агента

### Что сравнивать

1. **Тон и стиль:**
   - Формальность/неформальность
   - Сарказм/дружелюбие
   - Длина ответов

2. **Содержание:**
   - Глубина объяснений
   - Использование терминологии
   - Примеры и аналогии

3. **Формат:**
   - Структурированность ответов
   - Использование списков/таблиц
   - JSON/текст формат

4. **Контекст:**
   - Как агент использует историю диалога
   - Ссылки на предыдущие сообщения
   - Развитие темы

### Рекомендации по сравнению

- **Задавайте одинаковые вопросы** до и после смены промпта
- **Сохраняйте историю диалога** для анализа
- **Фиксируйте изменения** в логах (`logs/rick_bot.log`)
- **Делайте скриншоты** ответов для визуального сравнения

## 🐛 Troubleshooting

### Просмотр логов

```bash
# Последние логи
tail -50 logs/rick_bot.log

# Следить за логами в реальном времени
tail -f logs/rick_bot.log

# Поиск ошибок
grep ERROR logs/rick_bot.log
```

### Проблемы с изменением промпта

- **Промпт не применяется:** Убедитесь, что перезапустили бота после изменения кода
- **Ошибки формата:** Проверьте синтаксис XML в промпте
- **Неожиданные ответы:** Проверьте логи, возможно промпт не передаётся корректно

## 📚 Дополнительные ресурсы

- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт за 5 минут
- **[SCRIPTS_GUIDE.md](SCRIPTS_GUIDE.md)** - Руководство по скриптам запуска
- **[docs/CURRENT_IMPLEMENTATION.md](docs/CURRENT_IMPLEMENTATION.md)** - Описание текущей реализации

## 🔐 Безопасность

- **Никогда не коммитьте `.env` файл** в git
- Храните API ключи в безопасном месте
- Используйте переменные окружения для sensitive данных

---

**Удачи в экспериментах!** 🧪
