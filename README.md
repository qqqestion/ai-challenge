
# 🚀 День 26: Встроить локальную LLM в приложение (CLI, офлайн)

**Задача:** интегрировать локальную LLM в реальное приложение. В этом репозитории приложение — это CLI-утилита `ollama_chat.py`, которая ходит в **локальную Ollama** по HTTP API и отвечает на запросы пользователя **офлайн** (без внешних API).

**Модель для примеров:** `gpt-oss:20b`.

## ✅ Требования к результату

- **Локальная модель запущена** (через Ollama).
- **Приложение (CLI) отвечает на запросы пользователя офлайн**.
- **Есть проверка** (лучше — CLI и API).
- **Есть пример вопроса и ответа** (скрин/лог в терминале подойдёт).
- **Модель**: `gpt-oss:20b`.

## 🧩 Подготовка: Ollama

### 1) Установить Ollama

Установите Ollama удобным способом для вашей ОС, затем убедитесь, что CLI доступен:

```bash
ollama --version

```

### 2) Запустить локальный сервер Ollama

Обычно Ollama поднимает HTTP API на `http://localhost:11434`.

Проверка, что API жив:

```bash
curl -sS http://localhost:11434/api/tags

```

### 3) Скачивание (pull) модели

Для этого задания используем модель `gpt-oss:20b`.

- Посмотреть, что уже установлено:

```bash
ollama list

```

- Скачать модель:

```bash
ollama pull gpt-oss:20b

```

## 🖥️ Проверка через Ollama CLI

Запуск интерактивного режима:

```bash
ollama run gpt-oss:20b

```

Пример вопросов для проверки:
- «Объясни разницу между TCP и UDP в 3 пунктах».
- «Сгенерируй короткий TODO-лист на день».

## 🌐 Проверка через API (curl)

Ollama API endpoint: `POST /api/generate`.

```bash
curl -sS http://localhost:11434/api/generate \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "gpt-oss:20b",
    "prompt": "Скажи одним абзацем, что такое RAG",
    "stream": false
  }'

```

В ответе ожидается JSON, где есть поле `response` со сгенерированным текстом.

## 🧪 Реальное приложение: CLI-утилита из этого репозитория

В репозитории есть `ollama_chat.py` — CLI-утилита, которая ходит в локальную Ollama по HTTP API (`/api/generate`) и выводит ответ в терминал.

Запуск:

```bash
python3 ollama_chat.py --model gpt-oss:20b

```

Пример сессии:

```text
> Привет! Сформулируй 3 идеи, как проверить локальную LLM
... (ответ модели) ...
> /exit

```

Подсказки:
- По умолчанию в `ollama_chat.py` модель задана как `gpt-oss:20b`. Если у вас установлена другая — передайте её через `--model`.
- Если модель отвечает долго — увеличьте таймаут:

```bash
python3 ollama_chat.py --model gpt-oss:20b --timeout 180

```

## 🧯 Troubleshooting

- **`curl: (7) Failed to connect` / клиент не подключается**
  - Убедитесь, что Ollama запущена и слушает `localhost:11434`.
  - Проверьте `curl -sS http://localhost:11434/api/tags`.

- **Ошибка “model not found”**
  - Проверьте `ollama list`.
  - Скачайте модель: `ollama pull gpt-oss:20b`.

- **Таймауты**
  - Увеличьте `--timeout` в `ollama_chat.py`.

## 🧪 Как проверить (чеклист)

1. Убедиться, что Ollama доступна: `ollama --version`.
2. Убедиться, что API отвечает: `curl -sS http://localhost:11434/api/tags`.
3. Проверить CLI: `ollama run gpt-oss:20b` → задать вопрос → получить ответ.
4. Проверить API: `curl ... /api/generate` → получить JSON с `response`.
5. Проверить приложение из репозитория: `python3 ollama_chat.py --model gpt-oss:20b` → задать вопрос → получить ответ.

## 🧾 Что приложить в качестве результата

- Лог/скрин, где видно:
  - `curl -sS http://localhost:11434/api/tags` (сервер жив)
  - `python3 ollama_chat.py --model gpt-oss:20b` + 1-2 вопроса и ответы

