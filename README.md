
# 🚀 День 20: Ассистент разработчика

**Описание:** Добавляем ассистента, который понимает проектную документацию через RAG, знает контекст git-репозитория через MCP и отвечает на `/help` с подсказками, цитируя код и правила стиля.

✨ **Что нового:**
- 🆕 RAG, подключённый к проектным материалам (README, API, схемы данных, py_docs).
- 🆕 MCP-интеграция с текущим git-репозиторием (ветка, статус, изменённые файлы).
- 🆕 Команда `/help`, отвечающая на вопросы о проекте с фрагментами кода и ссылками на правила стиля.
- 🧪 Логи/метрики по источникам контекста и качеству ответов.

🎯 **Ключевая фича:** Контекстный ассистент, который сочетает RAG по внутренним документам и осведомлённость о состоянии репозитория через MCP, выдавая проверяемые подсказки.

📚 **Полезно почитать:**
- Практики RAG (индексация, схемы данных, обновление векторов)
- MCP-адаптеры для git/файловой системы
- Гайды по проектным соглашениям и стилю кода

## 📖 Описание задания

Задача: «День 20. Ассистент разработчика» — подключить ассистента к документации проекта через RAG, дать ему доступ к git-контексту через MCP и реализовать команду `/help`, которая отвечает на вопросы о проекте, показывая релевантные фрагменты кода и правила.

## 🎯 Цели

1. Обеспечить RAG-доступ к проектным документам (README, API, схемы данных, py_docs).
2. Подключить MCP-сервер для git-контекста (текущая ветка, статус, изменённые/открытые файлы).
3. Реализовать команду `/help`, возвращающую ответ + ссылки на исходники/правила стиля.
4. Обеспечить метрики/логи по источникам контекста и качеству ответов.
5. Обработать кейсы без релевантного контекста (fallback/предупреждение).

## 🔧 План работ (ожидаемое)

- Собрать/обновить векторное хранилище по проектной документации (README, py_docs, схемы данных).
- Настроить RAG-пайплайн: поиск, отбор контекста, форматирование ссылок на файлы/строки.
- Подключить MCP git-сервер: текущая ветка, статус, список изменённых и открытых файлов.
- Реализовать `/help`:
  - Принимает вопрос.
  - Достаёт контекст из RAG + git (если нужно: ветка, diff, открытые файлы).
  - Формирует ответ с цитированием фрагментов кода/правил.
- Добавить метрики/логи: количество найденных документов, источники (RAG/MCP), длина контекста, признаки пустого ответа.
- (Опционально) CLI/флаг для управления источниками (`--with-git`, `--docs-only`) и лимитом top-k.

## 🆚 Проверка ассистента

| Критерий | Без ассистента | С ассистентом (RAG+MCP) | Комментарий |
| --- | --- | --- | --- |
| Релевантность ответа |  |  |  |
| Наличие ссылок/цитат |  |  |  |
| Учет ветки/статуса git |  |  |  |
| Полезность для разработки |  |  |  |
| Время ответа |  |  |  |
| Итоговый вердикт |  |  |  |

| Запрос | Ожидаемый ответ `/help` | Источник контекста | Проверка |
| --- | --- | --- | --- |
| Как запустить проект? | Команда/шаги + ссылка на run.md | RAG (py_docs/run.md) | Шаги совпадают |
| Какая ветка сейчас? | Название ветки | MCP (git) | Совпадает с `git status` |
| Где лежит сервер RAG? | Путь к файлу + краткое описание | RAG (rag/server.py) | Путь и описание точны |

## 🧪 Как проверить

1. Обновить или сгенерировать векторную базу по актуальным докам (README, py_docs, схемы данных).
2. Поднять ассистента с включёнными RAG и MCP (git) источниками.
3. Выполнить `/help "Как запустить проект?"` — ответ должен ссылаться на соответствующие файлы/шаги.
4. Выполнить `/help "Какая ветка сейчас?"` — ответ должен совпасть с реальным `git status`.
5. Задать вопрос без контекста в доках — получить fallback/предупреждение и пустой/минимальный контекст.
6. Проверить логи: какие источники использованы (RAG/MCP), сколько документов и символов попало в контекст, время ответа.

