
# 🚀 День 18: Реранкинг и фильтрация

**Описание:** Добавляем второй этап после поиска: реранкер или фильтр релевантности (порог похожести или сторонняя модель). Сравниваем ответы без фильтра и с фильтром, настраиваем отсечение нерелевантных результатов.

✨ **Что нового:**
- 🆕 Второй этап «rerank/filter» после поиска (cross-encoder/пересчёт похожести или жёсткий порог).
- 🆕 Конфигурируемый порог отсечения и/или top-k для контекста.
- 🆕 Два пути сравнения: «как есть» (без фильтра) и «с фильтром/реранкингом».
- 🧪 Логи метрик (оценка релевантности, сколько чанков отсеяно, финальная длина контекста).

🎯 **Ключевая фича:** Улучшенный RAG с фильтрацией/реранкингом, уменьшающий шум и нерелевантные куски в контексте.

📚 **Полезно почитать:**
- Примеры RAG-пайплайнов (LangChain, LlamaIndex) и практики сравнения ответов
- Реранкеры (cross-encoder) и гайды по настройке порога similarity

## 📖 Описание задания

Задача: «День 18. Реранкинг и фильтрация» — добавить второй этап после поиска (reranker или фильтр по порогу), сравнить качество ответов без фильтра и с фильтром, настроить отсечение нерелевантных результатов. Результат: улучшенный RAG с фильтрацией/реранкингом.

## 🎯 Цели

1. Добавить стадию «rerank/filter» сразу после первоначального поиска.
2. Настроить порог отсечения нерелевантных результатов (и/или top-k) через конфиг.
3. Сохранить возможность ответа без фильтра для сравнения.
4. Сравнить ответы: где фильтр улучшил/не изменил/ухудшил результат; зафиксировать метрики.
5. Обработать кейс, когда все результаты отсеяны (fallback или предупреждение).

## 🔧 План работ (ожидаемое)

- Выбрать стратегию: reranker (cross-encoder/модель) или фильтр по коэффициенту похожести.
- Добавить конфиг для порога/топ-k и переключателя «with_filter/rerank vs baseline».
- После поиска пересчитать релевантность и отбросить результаты ниже порога (или взять топ-k).
- Собрать финальный контекст (отфильтрованные чанки) и запросить LLM.
- Сохранить логи: порог, количество найденных/отброшенных чанков, итоговую длину контекста.
- Сравнить ответы без фильтра и с фильтром; зафиксировать наблюдения в таблицах.
- (Опционально) Добавить CLI/флаг для выбора режима и порога.

## 🆚 Сравнение: без фильтра/реранка vs с фильтром/реранком

| Критерий | Без фильтра | С фильтром/реранком | Комментарий |
| --- | --- | --- | --- |
| Точность/релевантность |  |  |  |
| Полнота ответа |  |  |  |
| Цитируемость/ссылки |  |  |  |
| Уверенность модели |  |  |  |
| Артефакты/галлюцинации |  |  |  |
| Время ответа (с учётом реранкера) |  |  |  |
| Итоговый вердикт |  |  |  |

| Вопрос | Краткий ответ без фильтра | Краткий ответ с фильтром/реранком | Вывод |
| --- | --- | --- | --- |
|  |  |  |  |
|  |  |  |  |

## 🧪 Как проверить

1. Подготовить набор тестовых вопросов по корпусу.
2. Запустить baseline (без фильтра), сохранить ответ, список чанков и метрики.
3. Запустить режим с фильтром/реранком, сохранить ответ, отфильтрованные чанки и метрики.
4. Варьировать порог/топ-k, убедиться, что отсечение работает и не даёт пустого контекста без fallback.
5. Заполнить таблицы сравнения (качество, примеры вопросов) и отметить, где фильтрация дала выигрыш/регрессию.
6. Проверить, что переключатель режима и конфиг порога доступны и корректно работают.
