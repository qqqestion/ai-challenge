
# 🚀 День 16: Индексация документов

**Описание:** Собираем корпус документов (README, статьи, код или pdf→текст), режем на чанки, считаем эмбеддинги и сохраняем индекс для дальнейшего поиска/QA.

✨ **Что нового:**
- ✅ Пайплайн подготовки корпуса из разных источников (README/статьи/код/PDF→текст)
- ✅ Разбиение текста на чанки с метаданными
- ✅ Генерация эмбеддингов выбранной моделью
- ✅ Хранение индекса (FAISS, SQLite с векторами или JSON)

🎯 **Ключевая фича:** Готовый индекс документов с эмбеддингами для быстрых запросов.

📚 **Полезно почитать:**
- Примеры работы с библиотеками для эмбеддингов и индексов (FAISS, SQLite-вектора, JSON)

## 📖 Описание задания

Задача: «День 16. Индексация документов» — реализовать пайплайн: собрать документы, разбить их на чанки, сгенерировать эмбеддинги и сохранить индекс.

## 🎯 Цели

1. Собрать набор документов (README, статьи, код или pdf→текст).
2. Разбить текст на чанки с учётом размера и пересечений.
3. Сгенерировать эмбеддинги для каждого чанка.
4. Сохранить индекс (FAISS/SQLite с векторами/JSON) для последующего поиска.

## 🔧 План работ (ожидаемое)

- Подготовить конвейер загрузки и нормализации текстов из выбранных источников.
- Реализовать разбиение на чанки с хранением метаданных (источник, позиция).
- Добавить генерацию эмбеддингов для чанков выбранной моделью.
- Сохранить индекс (FAISS/SQLite/JSON) с возможностью последующей выборки.
- (Опционально) Добавить простой пример запроса к индексу.

## 🧪 Как проверить (черновик)

1. Подготовить небольшой набор документов разных типов.
2. Запустить пайплайн индексирования и убедиться, что на выходе сформированы чанки и их эмбеддинги.
3. Проверить, что индекс (FAISS/SQLite/JSON) сохраняется на диск.
4. Выполнить пробный поиск/запрос к индексу и убедиться, что результаты возвращаются.

