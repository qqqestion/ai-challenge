# 📜 Руководство по скриптам запуска

*urp* Всё что нужно знать о скриптах для запуска бота!

## 🚀 Доступные скрипты

### 1. `start.sh` - Полный запуск (рекомендуется) ⭐

**Что делает:**
- ✅ Проверяет наличие Python
- ✅ Создаёт виртуальное окружение (venv) если его нет
- ✅ Активирует venv
- ✅ Устанавливает/обновляет зависимости
- ✅ Проверяет наличие `.env` файла
- ✅ Запускает бота

**Первый запуск:**
```bash
# Сделать исполняемым (один раз)
chmod +x start.sh

# Запустить
./start.sh
```

**Последующие запуски:**
```bash
./start.sh
```

**Справка:**
```bash
./start.sh --help
```

**Только настройка (без запуска):**
```bash
./start.sh --setup
```

---

### 2. `quick-run.sh` - Быстрый запуск

**Что делает:**
- Активирует существующий venv
- Запускает бота

**Когда использовать:**
- Когда venv уже настроен
- Для быстрого перезапуска
- Когда не нужна проверка зависимостей

**Использование:**
```bash
chmod +x quick-run.sh  # Один раз
./quick-run.sh
```

⚠️ **Важно:** Требуется предварительный запуск `./start.sh` хотя бы один раз!

---

### 3. `run.py` - Прямой запуск Python

**Что делает:**
- Запускает бота напрямую через Python

**Когда использовать:**
- Когда venv уже активирован вручную
- Для запуска в IDE
- Для отладки

**Использование:**
```bash
# Активировать venv вручную
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Запустить
python run.py
```

---

## 🔄 Сравнение скриптов

| Скрипт | Создаёт venv | Устанавливает deps | Проверяет .env | Скорость |
|--------|--------------|-------------------|----------------|----------|
| `start.sh` | ✅ | ✅ | ✅ | Средняя |
| `quick-run.sh` | ❌ | ❌ | ❌ | Быстрая |
| `run.py` | ❌ | ❌ | ❌ | Быстрая |

---

## 📋 Типичные сценарии использования

### Сценарий 1: Первый запуск

```bash
# 1. Сделать скрипт исполняемым
chmod +x start.sh

# 2. Запустить (создаст venv, установит зависимости)
./start.sh

# Если .env не найден, создайте его:
nano .env  # или любой редактор

# 3. Запустить снова
./start.sh
```

### Сценарий 2: Ежедневное использование

```bash
# Быстрый запуск
./quick-run.sh

# Или полный (если обновились зависимости)
./start.sh
```

### Сценарий 3: После обновления кода

```bash
# Используйте start.sh чтобы обновить зависимости
./start.sh
```

### Сценарий 4: Разработка в IDE

```bash
# Активировать venv
source venv/bin/activate

# Запустить в IDE или напрямую
python run.py
```

### Сценарий 5: Только настройка окружения

```bash
# Создать venv и установить зависимости без запуска
./start.sh --setup

# Позже запустить
./quick-run.sh
```

---

## 🐛 Troubleshooting

### Проблема: "Permission denied"

**Решение:**
```bash
chmod +x start.sh
chmod +x quick-run.sh
```

### Проблема: "Python не найден"

**Решение:**
- Установите Python 3.10+
- Ubuntu/Debian: `sudo apt install python3 python3-venv`
- macOS: `brew install python3`

### Проблема: "Не удалось создать venv"

**Решение Ubuntu/Debian:**
```bash
sudo apt install python3-venv
```

**Решение macOS:**
```bash
# Переустановить Python через brew
brew reinstall python3
```

### Проблема: "ModuleNotFoundError"

**Решение:**
```bash
# Переустановить зависимости
./start.sh --setup

# Или вручную
source venv/bin/activate
pip install -r requirements.txt
```

### Проблема: "Файл .env не найден"

**Решение:**
```bash
# Создать .env файл
touch .env

# Заполнить обязательные переменные
nano .env
```

См. пример в README.md или QUICKSTART.md

---

## ⚙️ Переменные окружения в скриптах

Скрипты проверяют наличие следующих обязательных переменных в `.env`:

- ✅ `TELEGRAM_BOT_TOKEN`
- ✅ `YANDEX_API_KEY`
- ✅ `YANDEX_FOLDER_ID`

Если хотя бы одна не указана, скрипт остановится с ошибкой.

---

## 🎨 Цветной вывод

`start.sh` использует цветной вывод для удобства:

- 🔵 **[INFO]** - Информационные сообщения
- 🟢 **[SUCCESS]** - Успешные операции
- 🟡 **[WARNING]** - Предупреждения
- 🔴 **[ERROR]** - Ошибки

---

## 📊 Что происходит при запуске start.sh

```
1. [INFO] Проверка Python ✓
2. [INFO] Создание venv (если нужно) ✓
3. [INFO] Активация venv ✓
4. [INFO] Обновление pip ✓
5. [INFO] Установка зависимостей ✓
6. [INFO] Проверка .env ✓
7. [SUCCESS] Инициализация завершена ✓
8. [INFO] Запуск бота...

🧪 Rick Sanchez Bot запущен!
```

---

## 🔧 Расширенное использование

### Запуск с пользовательскими параметрами

Если нужно передать параметры в Python скрипт:

```bash
source venv/bin/activate
python run.py --custom-arg value
```

### Запуск в фоновом режиме

```bash
# Linux/Mac
nohup ./quick-run.sh > bot.log 2>&1 &

# Остановить
pkill -f "python run.py"
```

### Запуск с автоперезапуском

Создайте `run-forever.sh`:

```bash
#!/bin/bash
while true; do
    ./quick-run.sh
    echo "Bot stopped. Restarting in 5 seconds..."
    sleep 5
done
```

### Запуск через systemd (Linux)

Создайте `/etc/systemd/system/rick-bot.service`:

```ini
[Unit]
Description=Rick Sanchez Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/ai-challenge
ExecStart=/path/to/ai-challenge/quick-run.sh
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable rick-bot
sudo systemctl start rick-bot
```

---

## 📝 Рекомендации

### Для разработки
- Используйте `start.sh` для гарантии актуальности зависимостей
- Следите за логами в консоли
- `LOG_LEVEL=DEBUG` в `.env` для подробных логов

### Для продакшена
- Используйте systemd или supervisor для автоперезапуска
- Настройте логирование в файл
- `LOG_LEVEL=INFO` или `WARNING` в `.env`
- Регулярно обновляйте зависимости

---

*urp* Теперь у тебя есть все инструменты для запуска бота! Выбирай что подходит и вперёд!

**Wubba Lubba Dub Dub!** 🧪

