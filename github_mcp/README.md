
# GitHub MCP Сервер

Это MCP (Model Context Protocol) сервер для интеграции с GitHub через официальный REST API. Сервер предоставляет набор инструментов для получения информации о пользователях и репозиториях GitHub.

## Что такое MCP?

Model Context Protocol (MCP) — это открытый протокол, позволяющий AI-агентам взаимодействовать с различными источниками данных и инструментами. MCP сервер предоставляет набор инструментов, которые AI-агент может вызывать для выполнения различных задач.

## Доступные инструменты

### 1. `get_user`
Получение информации о пользователе GitHub.

**Параметры:**
- `username` (string, обязательный) — имя пользователя GitHub

**Пример ответа:**

```json
{
  "login": "octocat",
  "id": 1,
  "name": "The Octocat",
  "bio": "GitHub's mascot",
  "public_repos": 8,
  "followers": 9001,
  "following": 0,
  "html_url": "https://github.com/octocat",
  "avatar_url": "https://avatars.githubusercontent.com/u/583231?v=4",
  "created_at": "2011-01-25T18:44:36Z"
}

```

### 2. `get_user_repos`
Получение списка репозиториев пользователя.

**Параметры:**
- `username` (string, обязательный) — имя пользователя
- `limit` (number, опционально) — максимальное количество репозиториев (по умолчанию: 10, максимум: 100)

**Пример ответа:**

```json
{
  "username": "octocat",
  "total_count": 3,
  "repositories": [
    {
      "name": "Hello-World",
      "full_name": "octocat/Hello-World",
      "description": "My first repository",
      "html_url": "https://github.com/octocat/Hello-World",
      "stargazers_count": 80,
      "forks_count": 9,
      "language": "JavaScript",
      "open_issues_count": 0,
      "created_at": "2011-01-26T19:01:12Z",
      "updated_at": "2024-12-16T10:00:00Z",
      "default_branch": "main"
    }
  ]
}

```

### 3. `get_repo_info`
Получение детальной информации о репозитории.

**Параметры:**
- `owner` (string, обязательный) — владелец репозитория
- `repo` (string, обязательный) — название репозитория

**Пример ответа:**

```json
{
  "name": "Hello-World",
  "full_name": "octocat/Hello-World",
  "description": "My first repository",
  "html_url": "https://github.com/octocat/Hello-World",
  "stargazers_count": 80,
  "forks_count": 9,
  "language": "JavaScript",
  "open_issues_count": 0,
  "created_at": "2011-01-26T19:01:12Z",
  "updated_at": "2024-12-16T10:00:00Z",
  "default_branch": "main"
}

```

## Установка

### Предварительные требования
- Python 3.10 или выше
- pip
- GitHub Personal Access Token

### Создание GitHub Personal Access Token

1. Перейдите на https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Выберите необходимые разрешения (scopes):
   - `public_repo` — для доступа к публичным репозиториям
   - `read:user` — для чтения информации о пользователях
4. Сохраните токен в безопасном месте

### Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
GITHUB_PERSONAL_TOKEN=ghp_your_token_here

```

**Важно:** Никогда не коммитьте `.env` файл в Git!

### Установка зависимостей

```bash
cd github_mcp
pip install -r requirements.txt

```

Или установите в виртуальное окружение:

```bash
cd github_mcp
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt

```

## Запуск сервера

### Тестовый запуск (вручную)

```bash
cd github_mcp
python server.py

```

Сервер будет ожидать входящие MCP-запросы через stdio.

## Подключение к AI-агенту

### Подключение к Claude Desktop

1. **Найдите конфигурационный файл Claude Desktop:**

   - **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`
   - **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
   - **Linux:** `~/.config/Claude/claude_desktop_config.json`

2. **Откройте файл и добавьте конфигурацию MCP сервера:**

```json
{
  "mcpServers": {
    "github": {
      "command": "python",
      "args": [
        "/полный/путь/к/ai-challenge/github_mcp/server.py"
      ],
      "env": {
        "GITHUB_PERSONAL_TOKEN": "ghp_your_token_here"
      }
    }
  }
}

```

**Важно:**
- Замените `/полный/путь/к/ai-challenge` на реальный путь к вашему проекту
- Замените `ghp_your_token_here` на ваш GitHub токен

**Пример для macOS:**

```json
{
  "mcpServers": {
    "github": {
      "command": "python",
      "args": [
        "/Users/vchslv-mrzv/Programming/ai-challenge/github_mcp/server.py"
      ],
      "env": {
        "GITHUB_PERSONAL_TOKEN": "ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
      }
    }
  }
}

```

3. **Перезапустите Claude Desktop**

4. **Проверьте подключение:**
   - Откройте Claude Desktop
   - В нижней части окна должна появиться иконка инструментов (🔧)
   - Нажмите на неё, чтобы увидеть список доступных инструментов

### Использование виртуального окружения

Если вы хотите использовать виртуальное окружение для изоляции зависимостей:

```json
{
  "mcpServers": {
    "github": {
      "command": "/полный/путь/к/ai-challenge/github_mcp/venv/bin/python",
      "args": [
        "/полный/путь/к/ai-challenge/github_mcp/server.py"
      ],
      "env": {
        "GITHUB_PERSONAL_TOKEN": "ghp_your_token_here"
      }
    }
  }
}

```

### Подключение к другим MCP-клиентам

MCP протокол универсален и может быть использован с любым клиентом, поддерживающим MCP. Основные принципы:

1. Запустите `server.py` как подпроцесс
2. Используйте stdio для коммуникации
3. Следуйте спецификации MCP для обмена сообщениями
4. Передайте `GITHUB_PERSONAL_TOKEN` через переменные окружения

## Программная интеграция (API)

### Использование MCP клиента в Python коде

Вы можете интегрировать MCP сервер напрямую в свой Python код для программного доступа к инструментам.

#### Быстрый старт

**Пример 1: Базовый MCP клиент**

Запустите пример клиента для демонстрации всех инструментов:

```bash
cd github_mcp
python example_client.py

```

Этот скрипт демонстрирует:
- ✅ Подключение к MCP серверу
- ✅ Получение списка инструментов
- ✅ Вызов всех доступных инструментов
- ✅ Обработку результатов

**Пример 2: Интеграция с AI**

Запустите пример интеграции MCP с AI-помощником:

```bash
cd github_mcp
python integration_example.py

```

Этот скрипт показывает:
- ✅ Простой AI-помощник с доступом к MCP инструментам
- ✅ Анализ намерений пользователя
- ✅ Автоматический выбор и вызов нужных инструментов
- ✅ Форматирование результатов для пользователя

#### Пример кода

```python
import asyncio
import os
from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client

async def connect_and_use_mcp():

    # Параметры сервера
    server_params = StdioServerParameters(
        command="python",
        args=["github_mcp/server.py"],
        env={"GITHUB_PERSONAL_TOKEN": os.getenv("GITHUB_PERSONAL_TOKEN")}
    )

    # Подключение
    stdio_transport = await stdio_client(server_params)
    stdio, write = stdio_transport

    session = ClientSession(stdio, write)
    await session.initialize()

    try:

        # Получение списка инструментов
        tools = await session.list_tools()
        print(f"Доступно инструментов: {len(tools.tools)}")

        # Вызов инструмента
        result = await session.call_tool(
            "get_user",
            {"username": "octocat"}
        )
        print(result.content[0].text)

    finally:
        await session.__aexit__(None, None, None)

asyncio.run(connect_and_use_mcp())

```

### Интеграция с AI моделями

MCP инструменты можно использовать совместно с AI моделями через API:

```python
from src.llm.client import YandexLLMClient

# Создание AI клиента с доступом к MCP инструментам
llm = YandexLLMClient(api_key="...", base_url="...")
mcp_session = await connect_to_mcp_server()

# AI модель может использовать инструменты для получения данных
user_info = await mcp_session.call_tool("get_user", {"username": "octocat"})

# Передача результата в AI модель
response = await llm.send_prompt(
    messages=[
        {"role": "system", "content": "Ты помощник для работы с GitHub"},
        {"role": "user", "content": f"Проанализируй пользователя: {user_info}"}
    ],
    temperature=0.7
)

```

Подробнее см. в [основном README.md](../README.md#-программная-интеграция-mcp).

## Примеры использования

После подключения к Claude Desktop вы можете попросить Claude использовать инструменты:

**Пример 1:** "Покажи информацию о пользователе torvalds"
- Claude вызовет `get_user` с параметром `username: "torvalds"`

**Пример 2:** "Покажи репозитории пользователя octocat"
- Claude вызовет `get_user_repos` с параметром `username: "octocat"`

**Пример 3:** "Покажи информацию о репозитории facebook/react"
- Claude вызовет `get_repo_info` с параметрами `owner: "facebook"`, `repo: "react"`

## Отладка

### Включение логирования

Сервер автоматически логирует все вызовы инструментов. Для более детального логирования можете изменить уровень в `server.py`:

```python
logging.basicConfig(level=logging.DEBUG)

```

### Проверка соединения

Если инструменты не появляются в Claude Desktop:

1. Проверьте путь к `server.py` в конфигурации
2. Убедитесь, что Python доступен по указанному пути
3. Проверьте, что все зависимости установлены
4. Убедитесь, что `GITHUB_PERSONAL_TOKEN` правильно настроен
5. Перезапустите Claude Desktop
6. Посмотрите логи Claude Desktop (обычно в той же папке, что и конфигурация)

### Проверка работы API

Вы можете проверить работу GitHub API напрямую:

```bash
curl -H "Authorization: Bearer ghp_your_token" \
     -H "Accept: application/vnd.github+json" \
     -H "X-GitHub-Api-Version: 2022-11-28" \
     https://api.github.com/users/octocat

```

### Обработка ошибок

Сервер обрабатывает следующие типы ошибок:

- **404 Not Found** — пользователь или репозиторий не найден
- **403 Forbidden** — превышен rate limit или отказано в доступе
- **401 Unauthorized** — неверный или отсутствующий токен
- **Network errors** — проблемы с сетью или GitHub API недоступен

## GitHub API Rate Limits

GitHub API имеет ограничения на количество запросов:

- **С токеном:** 5000 запросов в час
- **Без токена:** 60 запросов в час

Текущий лимит можно проверить через API:

```bash
curl -H "Authorization: Bearer ghp_your_token" \
     https://api.github.com/rate_limit

```

## Структура проекта

```
github_mcp/
├── __init__.py          # Инициализация пакета
├── server.py            # Основной MCP сервер
├── tools.py             # Реализация инструментов с GitHub API
├── requirements.txt     # Зависимости Python
├── example_client.py    # Пример клиента
├── integration_example.py # Пример интеграции с AI
└── README.md           # Документация (этот файл)

```

## Требования

- Python 3.10+
- mcp >= 0.9.0
- pydantic >= 2.0.0
- httpx >= 0.27.0
- python-dotenv >= 1.0.0
- GitHub Personal Access Token

## Безопасность

⚠️ **Важные рекомендации по безопасности:**

1. **Никогда не коммитьте токены в Git**
2. Используйте `.env` файл для хранения токенов
3. Добавьте `.env` в `.gitignore`
4. Не передавайте токены в открытом виде
5. Регулярно обновляйте токены
6. Используйте токены с минимальными необходимыми правами

## Дальнейшее развитие

Возможные улучшения:

1. **Расширение функциональности:**
   - Создание issues
   - Работа с pull requests
   - Управление репозиториями
   - Работа с GitHub Actions
   - Поиск репозиториев

2. **Кэширование данных:**
   - Локальное кэширование для снижения нагрузки на API
   - Управление rate limits

3. **Аутентификация:**
   - OAuth авторизация
   - Поддержка GitHub Apps

## Лицензия

Этот проект является частью ai-challenge.

## Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории проекта.

---

**Документация GitHub REST API:** https://docs.github.com/en/rest

